<UserControl x:Class="Presentations.View.StatusDetailView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Presentations.View"
        xmlns:vm="clr-namespace:Presentations.ViewModel">


    <Grid>
        <StackPanel Margin="10">
            <TextBlock Text="Status ID:"/>
            <TextBox Text="{Binding Id}" Margin="5"/>

            <TextBlock Text="Book ID:"/>
            <TextBox Text="{Binding BookId}" Margin="5"/>

            <TextBlock Text="Availability:"/>
            <CheckBox IsChecked="{Binding Availability}" Margin="5"/>
        </StackPanel>
    </Grid>
</UserControl>
