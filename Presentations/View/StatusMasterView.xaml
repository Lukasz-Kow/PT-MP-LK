<UserControl x:Class="Presentations.View.StatusMasterView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Presentations.View"
        xmlns:vm="clr-namespace:Presentations.ViewModel">


    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Content="Add Status" Command="{Binding AddStatusCommand}" Margin="10"/>
        <Button Content="Delete Status" Command="{Binding DeleteStatusCommand}" Margin="10" Grid.Row="1"/>

        <ListBox ItemsSource="{Binding Statuses}" SelectedItem="{Binding SelectedStatus}" Margin="10" Grid.Row="1">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Id}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

    </Grid>
</UserControl>
