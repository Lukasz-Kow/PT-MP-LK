<UserControl x:Class="Presentations.View.CustomerDetailView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Presentations.View"
        xmlns:vm="clr-namespace:Presentations.ViewModel">


    <Grid>
        <StackPanel>
            <TextBlock Text="First Name:"/>
            <TextBox Text="{Binding FirstName, Mode=TwoWay}" Margin="5"/>

            <TextBlock Text="Last Name:"/>
            <TextBox Text="{Binding LastName, Mode=TwoWay}" Margin="5"/>

            <TextBlock Text="ID:"/>
            <TextBox Text="{Binding Id, Mode=TwoWay}" Margin="5"/>

            <TextBlock Text="Age:"/>
            <TextBox Text="{Binding Age, Mode=TwoWay}" Margin="5"/>

            <TextBlock Text="Address:"/>
            <TextBox Text="{Binding Address, Mode=TwoWay}" Margin="5"/>

            <TextBlock Text="City:"/>
            <TextBox Text="{Binding City, Mode=TwoWay}" Margin="5"/>
        </StackPanel>
    </Grid>
</UserControl>
