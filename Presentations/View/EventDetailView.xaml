<UserControl x:Class="Presentations.View.EventDetailView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Presentations.View"
        xmlns:vm="clr-namespace:Presentations.ViewModel">



    <Grid>
        <StackPanel Margin="10">
            <TextBlock Text="Event ID:"/>
            <TextBox Text="{Binding Id}" Margin="5"/>

            <TextBlock Text="Status ID:"/>
            <TextBox Text="{Binding StatusId}" Margin="5"/>

            <TextBlock Text="Customer ID:"/>
            <TextBox Text="{Binding CustomerId}" Margin="5"/>

            <TextBlock Text="Time:"/>
            <DatePicker SelectedDate="{Binding Time}" Margin="5"/>

            <TextBlock Text="Reason:"/>
            <TextBox Text="{Binding Reason}" Margin="5"/>

            <TextBlock Text="Description:"/>
            <TextBox Text="{Binding Description}" Margin="5"/>
        </StackPanel>
    </Grid>
</UserControl>
